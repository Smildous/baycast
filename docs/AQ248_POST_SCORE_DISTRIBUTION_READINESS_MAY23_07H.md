# AQ-248 Post-score distribution readiness, May 23 07h

This pack is only for the moment after the first public Baycast settlement is live and verifiable. Not before. The proof has to be visible, the score has to be stable, and the link has to work without a private explanation.

Baycast is prediction polling, not gambling. The point to share is simple: people can make a public call, come back after the event, and see how the score settled.

## Send gate

Send only when all of this is true:

- one public market has settled and the result page is reachable from a clean browser session
- the score shown in the product matches the settled outcome
- the share URL opens without auth, staging hints, console errors, or dead images
- the copy says prediction polling, not betting, wagering, odds, casino, alpha, moon, or guaranteed signal
- the post can point to the settled example, not to a promise that settlement is coming
- the person sending has a real account and can reply in the same thread within the next hour
- every target is hand picked from prior context, not scraped, bought, guessed, or invented
- nothing is sent from this VPS by script, bot, cron, or unauthenticated CLI

If any line is false, wait.

## No-send rules

Do not send if the first public settlement is not done yet.

Do not send a broad launch post. This is a small proof note, sent to people who already care about forecasting, prediction markets, polls, sports/media outcomes, civic questions, or product experiments.

Do not claim traction, users, revenue, demand, community, growth, or inbound unless there is a public source to point at.

Do not say Baycast is a gambling product. Do not invite deposits. Do not compare it to betting apps.

Do not post from a fresh or borrowed account. Do not use a founder's name unless the founder is actually sending it.

Do not paste the same text into multiple communities. If it would look weird with the target's name removed, rewrite it.

Do not argue with skeptics. Answer once with the settled link and the product framing. If the thread turns into spam, stop.

## Five short DM variants

1. Hey, quick one. Baycast has its first public settled prediction now. It is prediction polling, not gambling: make a call, outcome happens, score settles. If that is your kind of thing, I would value a blunt read: [link]

2. We finally have a public Baycast example that settled end to end. No traction claim, just the proof loop working. Would you take a look and tell me where the framing is unclear? [link]

3. Small follow-up from Baycast. The first public score is settled, so there is now something real to inspect instead of a pitch. The core idea is public prediction polling with a visible score after the event. [link]

4. Thought this might be relevant to your forecasting/polling interest. Baycast just settled its first public prediction. I am looking for feedback on whether the result page makes the loop obvious. [link]

5. Not a launch blast, just asking a few people for a read after the first settlement. Baycast lets people make public predictions and then shows the settled score. If you have two minutes: [link]

## Three HN/Reddit comment variants

1. I am working on Baycast. The closest framing is prediction polling, not gambling: people make a public call, then the page settles after the event and shows the score. We waited to share until there was a settled public example, because otherwise it is just a pitch. Link: [link]

2. One thing I wanted to avoid with Baycast was the vague “forecasting community” claim before anything had resolved. The first public item is settled now, so the useful feedback is whether the settled page explains the loop clearly: prediction, outcome, score. [link]

3. Built this because polls are easy to answer but usually have no memory. Baycast adds the memory part: public prediction first, settlement later, visible score after. This is the first settled public example. Happy to hear what feels confusing. [link]

## Founder note

We waited to talk about Baycast until there was a public settlement to point at.

Baycast is prediction polling. You make a call before something happens, then the outcome settles and the score is visible after. No gambling framing, no traction claim, no fake launch energy.

The first public settled example is here: [link]

If you care about forecasting, polls, or products where public answers should have memory, I would appreciate a direct read on whether the result page makes the idea clear.

## Channels still blocked on this VPS

Rechecked from the shell on May 23 at 07h. This machine must not send distribution messages.

Blocked:

- X/Twitter: no `twurl` or `t` CLI present, no authenticated X posting path verified
- email: no `mail`, `mailx`, `sendmail`, `msmtp`, `mutt`, `neomutt`, `swaks`, `aws`, `ses`, or `aws-cli` present
- Slack: no `slack` or `slack-cli` present, only bot-related environment key names exist and no outbound posting CLI is available
- Discord: no `discord` or `discord-cli` present, only bot-related environment key names exist and no outbound posting CLI is available
- Telegram: no `telegram`, `telegram-cli`, or `tg` present, only bot-related environment key names exist and no outbound posting CLI is available
- Hacker News: no `hn`, `haxor-news`, or `hackernews` CLI present
- Reddit: no `reddit`, `reddit-cli`, `tuir`, `rtv`, or `praw` CLI present
- Mastodon/Bluesky: no `mastodon`, `toot`, `bluesky`, `bsky`, or `atproto` CLI present

Available but not a send path for this task:

- `curl`, `wget`, `node`, `npm`, `npx`, `pnpm`, and `python3` are present, but using them would require credentials or custom posting code and would violate the no-send constraint
- `gh` is present and authenticated for GitHub repo work, but GitHub is not an approved distribution channel here
- `xdg-open` and `open` are present, but they do not create an authenticated outbound channel

Private warm target list: not available on this VPS in the task context. Do not invent recipients.

## Ready-to-use placeholders

Replace before any real send:

- `[link]` with the first public settled Baycast URL
- `I` with the actual sender if not the founder
- any community-specific wording with the thread context

Leave the message unsent until the gate above passes.
